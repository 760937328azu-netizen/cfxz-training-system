# 长发小寨新人培训系统 — 完整架构报告

> 生成时间：2026-07-26 23:15  
> 用途：供 Codex 做全系统检查  
> 当前 Git HEAD：`3e234b7` fix: 修复关卡顺序解锁失效

---

## 一、系统概览

| 项目 | 内容 |
|------|------|
| **系统名称** | 长发小寨新员工培训系统 |
| **技术栈（前端）** | React 19 + Vite 8 + TypeScript 6 + Tailwind CSS v4 |
| **技术栈（后端）** | Node.js 22 + Express 4 + Prisma 6 + PostgreSQL (Supabase) |
| **认证方式** | JWT（员工 `cfxz-auth-token` / 管理员 `cfxz-admin-auth-token`） |
| **部署方式** | Docker 多阶段构建 → Railway（GitHub 仓库自动部署） |
| **公网地址** | `https://cfxz-training-system-production.up.railway.app` |
| **GitHub 仓库** | `https://github.com/760937328azu-netizen/cfxz-training-system.git` |
| **分支** | `master` |

---

## 二、目录结构与文件清单

### 2.1 前端源码 (`src/`)

```
src/
├── main.tsx                          # 应用入口
├── App.tsx                           # 主应用组件（路由守卫、布局编排）
├── App.css / index.css / design-system.css  # 全局样式
├── lib/
│   └── api.ts                        # API 客户端层（前后端通信唯一入口）
├── hooks/
│   ├── useCurrentUser.ts             # 用户身份统一入口
│   ├── useHashRoute.ts               # Hash 路由解析
│   └── useLearningProgress.ts        # 学习进度管理（API + localStorage 双模式）
├── data/
│   ├── learningData.ts               # 6 关卡数据定义
│   ├── cultureStageData.ts           # 文化馆 6 展区数据
│   ├── certificationData.ts          # 认证考试题库
│   ├── rulesGameData.ts              # 第五关 4 个游戏数据
│   ├── handbook-kb.json              # 员工手册知识库（52 章节，17535 字）
│   ├── v4ContentPlan.ts              # 内容计划
│   └── voiceCatalog.ts               # 语音引导目录
├── utils/
│   └── handbookQA.ts                 # 员工手册 Q&A 引擎（17 意图匹配器）
├── components/
│   ├── GrowthMap.tsx                 # 成长地图（6 节点 SVG 路径）
│   ├── Sidebar.tsx                   # 左侧导航栏
│   ├── WelcomeArea.tsx               # 首页欢迎区
│   ├── CurrentStation.tsx            # 当前探索站
│   ├── MyTasks.tsx                   # 我的任务
│   ├── LearningWorldPreview.tsx      # 学习天地预告
│   ├── XiaoyaoCompanion.tsx          # 小瑶浮动助手（聊天面板）
│   ├── XiaoyaoDialogue.tsx           # 小瑶对话统一组件
│   ├── CultureXiaoyaoDialogue.tsx    # 文化馆专用小瑶对话
│   ├── VoiceGuide.tsx                # [已废弃] 旧音频播放器
│   └── rules/                        # 第五关 4 个游戏组件
│       ├── RocketBossGame.tsx        # 制度闯关
│       ├── ValueCatchGame.tsx        # 价值接接看
│       ├── ValueMatchGame.tsx        # 文化对对碰
│       └── RulesQuizGame.tsx         # 知识问答
├── pages/
│   ├── LoginPage.tsx                 # 前台登录页
│   ├── WelcomePage.tsx               # 第一关：欢迎加入
│   ├── CompanyPage.tsx               # 第二关：品牌起源
│   ├── CultureMuseumPage.tsx         # 第三关：云游长发博物馆
│   ├── ProductStagePage.tsx          # 第四关：认识产品与核心技术
│   ├── RulesStagePage.tsx            # 第五关：制度闯关
│   ├── CertificationPage.tsx         # 第六关：入职认证
│   ├── StageDetailPage.tsx           # 关卡详情通用页
│   ├── LearningWorldPage.tsx         # 学习天地（认证后解锁）
│   ├── TasksPage.tsx                 # 任务中心
│   ├── HistoryPage.tsx               # 学习历史
│   └── ProfilePage.tsx               # 个人中心
└── admin/                            # 后台管理面板
    ├── AdminApp.tsx                  # 后台主应用
    ├── admin.css                     # 后台样式
    ├── auth.ts                       # 后台认证逻辑
    ├── store.ts                      # 后台数据存储（localStorage 降级）
    ├── types.ts                      # 后台类型定义
    ├── seed.ts                       # 种子数据
    ├── components/
    │   ├── AdminShell.tsx            # 后台外壳布局
    │   ├── AdminDrawer.tsx           # 后台侧边抽屉
    │   ├── AdminTable.tsx            # 通用数据表格
    │   ├── EmployeeFormModal.tsx     # 员工表单弹窗
    │   ├── ConfirmModal.tsx          # 确认弹窗
    │   └── UI.tsx                    # UI 基础组件
    └── pages/
        ├── LoginPage.tsx             # 后台登录页
        ├── DashboardPage.tsx         # 数据看板
        ├── EmployeesPage.tsx         # 员工管理
        ├── BatchesPage.tsx           # 批次管理
        ├── ProgressPage.tsx          # 进度查看
        ├── CertificationPage.tsx     # 认证管理
        ├── HistoryPage.tsx           # 操作历史
        ├── ReportsPage.tsx           # 报表导出
        └── SettingsPage.tsx          # 系统设置
```

### 2.2 后端源码 (`server/`)

```
server/
├── package.json                      # 后端依赖
├── .env / .env.example / .env.production  # 环境变量（不提交 Git）
├── prisma/
│   └── schema.prisma                 # 数据库 Schema（11 张表）
├── src/
│   ├── index.ts                      # Express 应用入口
│   ├── seed.ts                       # 数据库种子（创建管理员 + 题库）
│   ├── lib/
│   │   ├── prisma.ts                 # Prisma Client 单例
│   │   ├── auth.ts                   # JWT 签发/验证
│   │   └── logger.ts                 # 日志工具
│   ├── middleware/
│   │   ├── auth.ts                   # JWT 认证中间件
│   │   ├── requireRole.ts            # 角色权限中间件
│   │   ├── rateLimiter.ts            # 限流中间件
│   │   └── errorHandler.ts           # 全局错误处理
│   └── routes/
│       ├── auth.routes.ts            # 认证路由
│       ├── employee.routes.ts        # 员工管理路由
│       ├── progress.routes.ts        # 学习进度路由
│       ├── certification.routes.ts   # 认证考试路由
│       ├── batch.routes.ts           # 批次管理路由
│       ├── admin.routes.ts           # 管理员路由
│       ├── moka.routes.ts            # Moka 集成路由
│       ├── export.routes.ts          # 数据导出路由
│       └── dashboard.routes.ts       # 数据看板路由
└── public/                           # 后端静态资源（与前端 public/ 镜像，Docker 构建时被前端 dist 覆盖）
```

---

## 三、数据存储位置一览

### 3.1 浏览器 localStorage 键值表

| Key | 用途 | 写入位置 | 读取位置 |
|-----|------|----------|----------|
| `cfxz-auth-token` | 员工 JWT token | `src/lib/api.ts` → `setEmployeeToken()` | `src/lib/api.ts` → `getEmployeeToken()` |
| `cfxz-admin-auth-token` | 管理员 JWT token | `src/lib/api.ts` → `setAdminToken()` | `src/lib/api.ts` → `getAdminToken()` |
| `cfxz-user-name` | 当前用户姓名 | `useCurrentUser.ts` → `saveCurrentUser()` | `useCurrentUser.ts` → `getCurrentUserName()` |
| `cfxz-user-department` | 当前用户部门 | 同上 | `getCurrentUserDepartment()` |
| `cfxz-user-username` | 登录账号（API 模式 = 手机号） | 同上 | `getCurrentUserUsername()` |
| `cfxz-user-employee-id` | 后台员工记录 UUID | 同上 | `getCurrentUserEmployeeId()` |
| `cfxz-user-phone` | 手机号 | 同上 | `getCurrentUserPhone()` |
| `cfxz-user-position` | 岗位 | 同上 | `getCurrentUserPosition()` |
| `cfxz-user-batch-name` | 培训批次名称 | 同上 | `getCurrentUserBatchName()` |
| `cfxz-user-entry-date` | 入职日期 | 同上 | — |
| `cfxz-login-session` | 登录会话标记（降级模式用 `"1"`） | 同上 | `isLoggedIn()` |
| `cfxz-phase3-learning-progress-v2-${username}` | 学习进度（按用户隔离） | `useLearningProgress.ts` → `writeProgress()` | `useLearningProgress.ts` → `loadProgress()` |
| `cfxz-phase3-learning-progress-v1` | [旧版] V1 进度（已自动迁移并删除） | — | 迁移逻辑在 `loadProgress()` |
| `cfxz-admin-store-v1` | [降级模式] 后台管理数据（员工/批次/进度） | `src/admin/store.ts` | `src/admin/store.ts` |

### 3.2 后端数据库（PostgreSQL / Supabase）

**连接方式：** Prisma ORM，`DATABASE_URL`（连接池 6543）+ `DIRECT_URL`（直连 5432）

**11 张数据表：**

| 表名 | Prisma Model | 用途 |
|------|-------------|------|
| `users` | `User` | 员工（新人）账号 |
| `admin_users` | `AdminUser` | 管理员账号 |
| `training_batches` | `TrainingBatch` | 培训批次 |
| `learning_progress` | `LearningProgress` | 学习进度总表（每员工一条） |
| `stage_progress` | `StageProgress` | 关卡进度（每员工每关一条） |
| `game_progress` | `GameProgress` | 游戏进度（第五关 4 个游戏） |
| `certification_attempts` | `CertificationAttempt` | 认证考试记录 |
| `learning_events` | `LearningEvent` | 学习事件日志 |
| `moka_sync_events` | `MokaSyncEvent` | Moka 同步事件 |
| `admin_operation_logs` | `AdminOperationLog` | 管理员操作日志 |
| `question_bank` | `QuestionBank` | 认证题库 |

### 3.3 静态资源存储 (`public/`)

| 路径 | 内容 | 数量 |
|------|------|------|
| `public/logo/` | 品牌 Logo（棕色人头、金色 3D 硬币、各尺寸 favicon） | 9 个文件 |
| `public/xiaoyao/` | 小瑶角色原图（带棋盘格背景，**禁止直接使用**） | 13 个 PNG |
| `public/xiaoyao/transparent/` | 小瑶透明底图（**所有页面应使用此目录**） | 13 个 PNG |
| `public/hairstyles/` | 三种发式人物图（桂中髻/螺丝发/乌龙盘发，uniform = 透明底） | 6 个 PNG |
| `public/products/` | 产品白底图（15 张 JPG） | 15 个 JPG |
| `public/stages/` | 关卡徽章图（6 关 + 基石场景） | 7 个文件 |
| `public/rice-steps/` | 淘米水工艺 5 步骤图 | 7 个 PNG |
| `public/welcome-icons/` | 首页欢迎区 4 个图标 | 4 个 PNG |
| `public/images/` | 内容插图（红瑶文化、logo 漫画、淘米水对比） | 4 个文件 |
| `public/assets/voice/` | 小瑶语音引导 MP3 | ~50 个 MP3 |
| `public/assets/videos/` | 公司介绍视频 | 1 个 MP4 |
| `public/assets/museum/` | 博物馆入口视频/海报 + 6 展区图 | 8 个文件 |
| `public/assets/growth-map/` | 成长地图微世界底图 | 2 个 PNG |
| `public/assets/` | 登录背景视频、头皮微生态图 | 4 个文件 |
| `public/legacy/` | [废弃] 旧火箭游戏 JS | 1 个文件 |

### 3.4 知识库数据文件

| 文件 | 用途 | 大小 |
|------|------|------|
| `src/data/handbook-kb.json` | 员工手册知识库（前端小瑶助手用） | 52 章节，17535 字 |
| `server/handbook-kb.json` | 员工手册知识库（后端副本，Docker 构建时复制） | 同上 |
| `src/data/certificationData.ts` | 认证考试题库（前端静态） | — |
| `src/data/cultureStageData.ts` | 文化馆 6 展区内容 | — |
| `src/data/rulesGameData.ts` | 第五关 4 个游戏题目数据 | — |
| `src/data/learningData.ts` | 6 关卡元数据（标题/描述/图标） | — |
| `src/data/voiceCatalog.ts` | 语音引导文件映射表 | — |

---

## 四、API 端点清单

### 4.1 认证 (`/api/auth`)

| 方法 | 路径 | 权限 | 用途 |
|------|------|------|------|
| POST | `/api/auth/employee/login` | 公开 | 员工登录（手机号 + 密码） |
| POST | `/api/auth/admin/login` | 公开 | 管理员登录 |
| GET | `/api/auth/me` | 已认证 | 获取当前用户信息 |
| POST | `/api/auth/change-password` | 已认证 | 修改密码 |

### 4.2 员工管理 (`/api/employees`)

| 方法 | 路径 | 权限 | 用途 |
|------|------|------|------|
| GET | `/api/employees` | 管理员 | 员工列表 |
| GET | `/api/employees/:id` | 管理员 | 员工详情 |
| POST | `/api/employees` | 超级管理员 | 创建员工 |
| PUT | `/api/employees/:id` | 超级管理员 | 更新员工 |
| DELETE | `/api/employees/:id` | 超级管理员 | 删除员工 |
| POST | `/api/employees/batch` | 超级管理员 | 批量导入员工 |
| POST | `/api/employees/:id/reset-progress` | 超级管理员 | 重置员工进度 |

### 4.3 学习进度 (`/api/progress`)

| 方法 | 路径 | 权限 | 用途 |
|------|------|------|------|
| GET | `/api/progress` | 已认证 | 获取自己的进度 |
| PUT | `/api/progress/stage` | 已认证 | 更新关卡进度 |
| PUT | `/api/progress/game` | 已认证 | 更新游戏进度 |
| PUT | `/api/progress/position` | 已认证 | 更新最后访问位置 |
| GET | `/api/progress/:userId` | 管理员 | 查看指定员工进度 |

### 4.4 认证考试 (`/api/certification`)

| 方法 | 路径 | 权限 | 用途 |
|------|------|------|------|
| GET | `/api/certification/questions` | 已认证 | 获取考题 |
| POST | `/api/certification/submit` | 已认证 | 提交考试 |
| GET | `/api/certification/history` | 已认证 | 考试历史 |
| POST | `/api/certification/:userId/reset` | 管理员 | 重置考试记录 |

### 4.5 其他路由

| 路由前缀 | 用途 |
|----------|------|
| `/api/batches` | 培训批次 CRUD |
| `/api/admin` | 管理员用户 CRUD + 操作日志 + 题库管理 |
| `/api/moka` | Moka HR 系统集成 webhook |
| `/api/export` | 数据导出（员工列表/进度报表） |
| `/api/dashboard` | 数据看板统计 |
| `/api/health` | 健康检查（无认证） |

---

## 五、核心业务逻辑

### 5.1 关卡顺序解锁机制

**关卡顺序（STAGE_ORDER）：**
```
welcome → company → culture → product → organization → certification
  第一关    第二关    第三关     第四关      第五关          第六关
```

**注意：** 前端 `STAGE_ORDER` 中第五关 ID 为 `"organization"`，但在 localStorage 存储和后端数据库中映射为 `"rules"`（`getProgressKey()` 中有映射逻辑）。

**状态计算（`getStageStatuses()`）：**
- `completed` — 该关 `progress[id].completed === true`
- `current` — 第一个未完成的关卡
- `locked` — 前一关未完成

**三层防护（2026-07-26 修复）：**
1. **GrowthMap.tsx** — locked 节点点击不导航，显示内联提示（3 秒消失）
2. **App.tsx** — `useEffect` 路由守卫，URL hash 直接访问 locked 关卡时重定向首页
3. **Sidebar.tsx** — 认证入口 locked 时不导航

### 5.2 双模式架构

| 模式 | 条件 | 数据存储 |
|------|------|----------|
| **API 模式** | `VITE_API_BASE_URL` 已配置 | 后端 PostgreSQL + JWT 认证 |
| **降级模式** | `VITE_API_BASE_URL` 未配置 | 纯 localStorage，无后端 |

**判断函数：** `isApiMode()` — 返回 `API_BASE_URL.length > 0`

**当前环境：**
- 本地开发 (`.env.local`)：`VITE_API_BASE_URL=http://localhost:4000/api` → API 模式
- 生产 (`.env.production`)：`VITE_API_BASE_URL=/api` → API 模式

### 5.3 用户身份读取

**统一入口：** `src/hooks/useCurrentUser.ts`

**规则：** 禁止在组件中直接 `localStorage.getItem("cfxz-user-name")`，必须使用：
- `useCurrentUser()` Hook（组件内）
- `getCurrentUserName()` / `getCurrentUserUsername()` 等同步函数（组件外）

### 5.4 进度 Key 对齐

**存储 Key：** `cfxz-phase3-learning-progress-v2-${username}`
- `username` 优先使用 `cfxz-user-username`（登录账号）
- 降级回退到 `cfxz-user-name`（姓名）
- API 模式下 `username` = 手机号，与后台 `Employee.phone` 对齐

---

## 六、部署架构

### 6.1 Docker 多阶段构建

```
Stage 1: frontend-builder (node:22-slim)
  → npm ci → npm run build → /app/frontend/dist

Stage 2: backend-builder (node:22-slim)
  → npm ci → npx prisma generate → npm run build → /app/server/dist

Stage 3: production (node:22-slim)
  → npm ci --only=production → npx prisma generate
  → COPY backend dist → COPY frontend dist as public
  → CMD: prisma db push → seed → node dist/index.js
```

### 6.2 环境变量

**前端：**
| 变量 | 本地 | 生产 |
|------|------|------|
| `VITE_API_BASE_URL` | `http://localhost:4000/api` | `/api` |

**后端（Railway 环境变量，不提交 Git）：**
| 变量 | 用途 |
|------|------|
| `DATABASE_URL` | Supabase 连接池 URL（端口 6543） |
| `DIRECT_URL` | Supabase 直连 URL（端口 5432，Prisma 迁移用） |
| `JWT_SECRET` | JWT 签名密钥 |
| `PORT` | 服务端口（默认 4000） |

### 6.3 健康检查

```
HEALTHCHECK --interval=30s --timeout=5s --start-period=120s --retries=3
  CMD fetch http://localhost:$PORT/api/health
```

### 6.4 静态文件服务

生产模式下，后端 Express 服务前端静态文件：
- 路径：`app.use(express.static(staticDir))`
- 前端 `dist/` 被复制到后端的 `public/` 目录
- 所有非 `/api` 开头的请求返回 `index.html`（SPA fallback）

---

## 七、登录凭证

### 7.1 管理后台
- **地址：** `https://cfxz-training-system-production.up.railway.app/#/admin`
- **账号：** `admin`
- **密码：** `Cfxz!Prod#2026Secure`

### 7.2 前台员工登录
- **地址：** `https://cfxz-training-system-production.up.railway.app/`
- **登录方式：** 手机号（登录账号）+ 密码
- **测试账号：** 需在后台创建员工后使用
- **验证逻辑：** `LoginPage` 验证后端 `Employee` 记录的 `phone` + `initialPassword` + `status === "active"`

---

## 八、已冻结布局 & 硬规则

### 8.1 Locked Layout（不可改结构）

| 模块 | 冻结内容 |
|------|----------|
| 成长地图 | 6 节点坐标 `[180,326,472,618,764,910]`，SVG `xMidYMid meet`，安全区左右各 ~180px |
| 文化馆三种发式 | `hairstyles` 数组必须用 `image` 字段绑定真实人物透明图，禁止编号占位 |
| 文化馆淘米水工艺 | 5 节点 SVG 贝塞尔曲线波浪路径，`stroke-dasharray` 进度动画 |

### 8.2 IP 使用规则

- 小瑶角色：入职导游 / 学习陪伴者，不是吉祥物
- 首屏只保留 1 个完整小瑶 + 1 个对话气泡
- **禁止使用** `public/xiaoyao/*.png`（带棋盘格背景），必须使用 `public/xiaoyao/transparent/*.png`
- 禁止将全身小瑶缩小放进圆形头像
- 小瑶对话统一使用 `XiaoyaoDialogue` 组件

### 8.3 Logo 规则

- **棕色人头** (`public/logo/cfxz-logo-brown.png`)：首页旋转、登录页、导航栏、文化馆
- **金色 3D 硬币** (`public/logo/cfxz-3d-coin.png`)：视频播放封面
- **绝对禁止** 用 AI 生成或替换 Logo
- 所有 Logo 必须透明底 PNG

### 8.4 小瑶助手可见性

- **所有页面无条件显示** XiaoyaoCompanion 浮动按钮（包括首页/01-04 关卡）
- 不要因"同视口重复小瑶"而隐藏

---

## 九、已知问题 & 注意事项

1. **项目根目录有大量临时文件** — 31 个 `dist-build-v*` 目录、数十个 `screenshot-*.png`、`*.cjs` 测试脚本。这些是开发过程中的临时验证产物，已在 `.dockerignore` 和 `.gitignore` 中排除，不影响生产部署，但建议清理。

2. **`*.cjs` 被 .gitignore 忽略** — 所有 `.cjs` 文件（验证脚本、测试脚本）不会被提交到 Git，但 `scripts/` 目录下的 `.cjs` 文件在本地存在。

3. **`server/public/` 被 .gitignore 忽略** — 后端的 `public/` 目录不提交 Git，Docker 构建时由前端 `dist/` 覆盖。

4. **Prisma `db push` 非致命** — Docker CMD 中 `prisma db push` 和 `seed` 设为 `|| echo 'WARNING...'`，即使失败也启动服务器。Supabase 连接超时（P1001）不影响运行（表结构在之前部署已建立）。

5. **前后端进度 Key 映射** — 前端 `STAGE_ORDER` 第五关 ID 为 `"organization"`，存储时映射为 `"rules"`。后端数据库 `StageProgress.stageId` 使用 `"rules"`。

6. **`VoiceGuide.tsx` 已废弃** — 不再被任何页面引用，但文件仍保留。

---

## 十、Git 提交历史（最近 15 条）

```
3e234b7 fix: 修复关卡顺序解锁失效 - locked状态点击/路由/Sidebar三层拦截
c0b157d feat: replace product PNG assets with white-background JPGs and update paths
16f8a59 fix: increase healthcheck start-period to 120s, use local prisma binary, add binaryTargets
39f939b fix: update lockfile for prisma dependency + add trust proxy for Railway
e83fa75 fix: run prisma db push + seed on container startup to create admin account and database tables
aaafb0e fix: mobile responsive layout for admin shell, admin drawer, xiaoyao companion, growth map
d282620 production: full deployment with public assets (images, xiaoyao, logo, stages)
90d4d4d fix: remove *.png from .gitignore so production assets upload to Railway
93b8b20 debug: add Dockerfile listing to inspect public/xiaoyao in build
659dad4 debug: add temporary /api/debug/files endpoint to inspect production public files
406dbba fix: remove *.png from .dockerignore so images are included in production build
aff0cd1 docs: add production deployment verification report - all 12 checks passed
c717436 fix: correct frontend dist path in Dockerfile + cleanup gitignore
12673f8 fix: Dockerfile prisma generate ordering + exclude env.production from docker context
5d13fdb docs: add deployment guide and launch report for production deployment
```
